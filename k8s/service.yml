apiVersion: v1
kind: Service
metadata:
  name: prioritizer-service
  labels:
    app: prioritizer-service
spec:
  type: ClusterIP
  selector:
    app: prioritizer-service
  ports:
    - protocol: TCP
      port: 9004
      targetPort: 9004